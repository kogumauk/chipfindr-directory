# Supabase Credentials - Replace with your actual project values
# These are exposed to the client-side because they are prefixed with PUBLIC_
PUBLIC_SUPABASE_URL="YOUR_SUPABASE_URL_HERE"
PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY_HERE"

# Add other environment-specific variables below as needed
# e.g., PUBLIC_API_ENDPOINT_STAGING="your_staging_api_url"
# e.g., SECRET_API_KEY="your_secret_server_side_key" (not prefixed with PUBLIC_)